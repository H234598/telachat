# Session 2026-05-26

## 0.49.0 Provider cleanup

- Removed the duplicate default `tki` provider from generated configs.
- Kept `tki` as a compatibility alias so old sessions and folder backends still
  resolve to the HuggingFace profile when they go through config validation.
- Renamed the visible HuggingFace model to `TKI` and mapped it internally to
  `Qwen/Qwen2.5-1.5B-Instruct` for API calls.
- Added per-profile `send_temperature` and `send_top_p` switches and disabled
  both by default for the OpenAI Responses profile to avoid GPT-5.x sampling
  parameter incompatibilities.
- Migrated the local user config after writing
  `~/.config/telachat/config.toml.bak-0.49.0-provider-cleanup`.

## Verification

- `PYTHONPATH=src python3 -m telachat profiles --json`
- `PYTHONPATH=src python3 -m unittest tests.test_config tests.test_client tests.test_cli.CliTests.test_init_and_profiles tests.test_cli.CliTests.test_folders_command_manages_system_prompts tests.test_controller.ControllerTests.test_folder_system_prompt_is_used_for_new_session -v`
- `make check`
- `make zipapp install`
- `~/.local/bin/telachat --version`
- `python3 dist/telachat.pyz --version`

## 0.50.0 Skill watchdog

- Added `telachat skill-watchdog` to compact oversized Codex Skill
  frontmatter descriptions while preserving the full Skill body.
- Tk and GTK gained startup support for the watchdog; later releases made this
  default-on and configurable from `config.toml`.

## 0.50.0 Verification

- `PYTHONPATH=src python3 -m telachat skill-watchdog --json`
- `PYTHONPATH=src python3 -m unittest tests.test_skill_watchdog tests.test_cli.CliTests.test_skill_watchdog_command_compacts_oversized_descriptions -v`
- `make check`

## 0.51.0 Theme expansion

- Added metallic/glass themes: `graphite-glass`, `liquid-chrome`, `black-ice`,
  and `brushed-steel`.
- Added aliases `glass`, `chrome`, `ice`, and `metallic`.
- Added the first metallic theme set while the Skill-watchdog startup contract
  was still being reconciled.

## 0.51.0 Verification

- `PYTHONPATH=src python3 -m unittest tests.test_skill_watchdog tests.test_themes -v`
- `make check`

## 0.51.1 Documentation correction

- Corrected docs that still described the Skill watchdog as disabled by
  default; the code starts it by default in Tk/GTK and supports
  `TELACHAT_DISABLE_SKILL_WATCHDOG=1` as opt-out.

## 0.52.0 GUI options

- Added persistent Tk/GTK options for theme, app icon, chat background,
  profile-header validation, and Skill-watchdog enablement.
- Imported the provided SVG icons and generated PNG assets for desktop use.
- Added `app_icon = "random"` hourly rotation and
  `skill_watchdog_enabled = false` as the config opt-out for the default-on
  Tk/GTK Skill watchdog.

## Post-0.52.0 correction

- Temporarily restored the safe default for new configs:
  `skill_watchdog_enabled = false`.
- Moved default prompt-template escaping out of f-string expressions so Python
  3.11 can compile `telachat.defaults`.

## 0.52.1 Safety release

- Released the Post-0.52.0 correction as `0.52.1` because `v0.52.0` was tagged
  before the Linux check exposed the Python 3.11 compile failure.

## 0.53.0 Explorer pass

- Added Tk folder rows in the left chat list, with double-click expand/collapse.
- Added a Tk right-click context menu for sessions, folders, and empty list
  space.
- Changed Tk and GTK New actions to request a conversation name before the
  session is created.
- Added the Skill-watchdog option to the Tk/GTK preferences flow.

## 0.53.1 Safety correction

- Restored the Skill watchdog's safe opt-in default after `0.53.0`.
- Reinstated the `TELACHAT_ENABLE_SKILL_WATCHDOG=1` guard on the low-level
  background start and restored tests for that contract.

## 0.54.0 Linux packaging

- Added `packaging/linux/install-telachat.sh`, a prefix-based installer that
  installs the release zipapp, launchers, manpages, a Freedesktop desktop entry,
  an icon, and an optional desktop shortcut.
- Added RPM build support through `packaging/rpm/telachat.spec` and
  `packaging/linux/build-rpm.sh`.
- Added `snap/snapcraft.yaml` for optional snap builds when Snapcraft is
  available.

## 0.54.1 Linux CI correction

- Changed `build-rpm.sh` to pass `--nodeps` so the RPM package can be built on
  non-RPM CI hosts that do not have Fedora/RHEL dependency metadata in the RPM
  database.
- Improved the Linux packaging regression test failure output for remote CI.

## 0.54.2 Linux CI correction

- Replaced non-POSIX shell brace expansion in `build-rpm.sh` with explicit
  directory arguments so Ubuntu/dash runners create the RPM source tree.

## 0.54.3 Linux packaging correction

- Skipped Linux shell launcher execution on Windows CI because generated POSIX
  shell scripts are not Windows executables.
- Escaped `$SNAP` in the Snapcraft wrapper rendering step so the generated
  launchers resolve the snap mount at runtime.

## 0.55.0 Copyable GUI errors

- Replaced non-selectable provider error message boxes in Tk and GTK with
  copyable error windows containing the full exception text and a copy button.
- Added GUI regression coverage so worker errors finish the operation and route
  through the copyable error surface.

## 0.56.0 Compact GUI headers

- Replaced the Tk and GTK chat-head action row with centered conversation
  titles and small left/right arrow toggles for the side panes.
- Added title double-click rename handling in both GUI frontends.
- Moved the visible sidebar `Regenerieren` action into the previous `Neu`
  button position while keeping new-session flows available through existing
  menu, slash, and context paths.
- Added `AGENTS.md` as a short contributor and coding-agent guide for the
  repository.

## 0.57.0 Shortcut help

- Added shared keyboard-shortcut help for CLI, Tk, and GTK through
  `/shortcuts` and alias `/keys`.
- Added `Ctrl+/` handling in the GUI prompt to open the shortcut overview.
- Confirmed that GitHub workflow dispatch currently returns HTTP 500 despite
  active workflows and a token with `workflow` scope; 0.56.0 release assets were
  uploaded manually from locally verified builds.

## 0.57.1 New sidebar action restore

- Restored the visible `Neu` sidebar quick action in Tk and GTK.
- Kept `Regenerieren` and `Check` visible beside it as quick actions.
- Added Tk row constants so the session list rowconfigure target stays aligned
  with the actual list row after the extra action row.

## 0.58.0 Prompt template management

- Added config/controller support to create/update, rename, and delete
  prompt templates without manually editing TOML.
- Added `telachat templates --set NAME TEXT`, `--rename OLD NEW`, and
  `--delete NAME`, including JSON output.
- Added GUI access: Tk exposes rename/delete through the template dropdown
  context menu; GTK exposes rename/delete buttons below the template dropdown.

## 0.59.0 GUI save-as-template

- Added Tk `Aus Eingabe speichern` in the template dropdown context menu.
- Added GTK `Speichern` in the template management button row.
- Both paths save the current composer text through the same
  `set_prompt_template` controller/config path as the CLI.

## 0.60.0 GUI template preview

- Added copyable prompt-template preview windows to both native GUIs.
- Tk exposes `Vorschau` from the template dropdown context menu.
- GTK exposes `Vorschau` next to `Einsetzen` below the template dropdown.

## 0.61.0 GUI template preview metadata

- Added shared `format_prompt_template_preview` metadata output with template
  name, character count, and supported variables.
- Tk and GTK now show that metadata above the template body in the copyable
  preview window.

## 0.62.0 CLI template preview

- Added `telachat templates --show NAME` to print the same metadata-rich
  template preview used by the GUI windows.
- Added `telachat templates --show NAME --json` for scripts that need one
  structured template record.

## 0.63.0 Custom template variables

- Added custom placeholder detection for prompt templates, reported as
  `custom_variables` in CLI JSON and as `Custom-Variablen` in previews.
- Added `telachat ask --template-var NAME=VALUE` so CLI template sends can fill
  custom placeholders such as `{topic}` while built-in variables stay reserved.

## 0.64.0 GUI custom template variable dialogs

- Added Tk and GTK value dialogs for custom prompt-template variables before a
  template is inserted into the composer.
- Made `--template-var` fail with a clear configuration error when used without
  `--template`, matching the sibling review hint.

## 0.64.1 Tk template variable form

- Replaced the Tk custom-template variable `askstring` loop with one modal form
  that collects all custom variable values before insertion.

## 0.65.0 GUI context completion

- Moved slash-command context completion into the shared command layer.
- Tk and GTK now complete values after commands such as `/provider`, `/model`,
  `/template`, `/theme`, `/move`, `/tag`, `/load`, `/sort`, and `/history`.
- Focused command, CLI, Tk, and GTK completion tests passed before full
  validation.

## 0.65.1 Safe installer smoke

- Added `make linux-installer-smoke` for temporary-prefix Linux installer
  checks that do not touch the real desktop shortcut.
- Added the same safe installer smoke to the Linux tag packaging workflow.

## 0.66.0 GUI template value history

- Added session-local custom prompt-template value history shared by Tk and GTK.
- Successful GUI template insertion remembers custom variable values per
  template and prefills the next dialog for that template.

## 0.67.0 Redacted config view

- Added `telachat config-check --show-redacted` for a TOML-shaped support view
  of the active config.
- The view reuses the backup redaction writer so raw API keys and auth headers
  stay hidden while non-secret settings remain inspectable.

## 0.67.1 Redacted config profile filter

- Integrated the correction PR for `config-check --profile NAME --show-redacted`.
- The redacted TOML output now includes only the selected profile section when
  a profile filter is active.

## 0.68.0 Latest response copy button

- Added a compact copy button to the Tk and GTK chat headers.
- The button copies the latest assistant response and reports when no assistant
  response exists yet.
- Added shared latest-assistant-content selection logic and GUI regression
  coverage.

## 0.69.0 Portable folder bundles

- Added `telachat export-folder --bundle`, which writes a ZIP with
  `folder.json` plus a small manifest.
- `telachat import-folder` can now read those ZIP bundles directly.
- The bundle path intentionally excludes config files, env files, provider
  secrets, and API keys; only folder/session metadata and chat messages are
  included.

## 0.69.1 Non-UTF-8 import errors

- Integrated PR #8 from the correction branch.
- `import-session` and `import-folder` now report a clear ConfigError when the
  input file, or a bundle's `folder.json`, is not UTF-8 encoded.
- Added regression tests for non-UTF-8 JSON session imports and folder ZIP
  bundles.

## 0.70.0 Folder context notes

- Added per-folder context notes stored in SQLite.
- New chats in a folder append that note below the folder/default system
  prompt.
- `telachat folders --context`, `--set-context`, and `--show-context` expose
  the field in CLI workflows.
- Folder JSON and ZIP exports preserve the context note for portable imports.

## 0.70.1 Folder prompt editing guard

- Integrated the correction branch that keeps folder context notes out of the
  editable Tk/GTK folder prompt fields.
- New chats still apply the context note when the selected folder base prompt
  is used.
- Saving a GUI folder prompt strips an exact effective context suffix before
  persisting the folder system prompt.

## 0.70.2 Legacy folder context normalization

- Integrated the correction branch that normalizes legacy folder prompts which
  already contain the exact folder context suffix.
- GUI edit flows still show only the base prompt for those legacy values.
- New sessions avoid appending the same folder context twice.

## 0.70.3 GitHub Actions Node 24 opt-in

- Set `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` in the Linux workflow so JavaScript
  actions run under Node 24 before GitHub's Node 20 removal.
- Added workflow coverage so the opt-in remains visible in tests.

## 0.70.4 Windows Node 24 opt-in

- Extended the same Node 24 opt-in to the Windows workflow, which also uses
  JavaScript actions for setup-python and artifact upload.
- Added workflow coverage so any workflow with `uses:` steps must carry the
  opt-in.

## 0.70.5 Folder prompt wording

- Renamed the Tk/GTK folder prompt action from `Ordner-Prompt` to
  `Prompt speichern`.
- Updated GUI status text and docs so the action reads as saving the visible
  system prompt as the selected folder's prompt.
