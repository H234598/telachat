# Session 2026-05-26

## 0.49.0 Provider cleanup

- Removed the duplicate default `tki` provider from generated configs.
- Kept `tki` as a compatibility alias so old sessions and folder backends still
  resolve to the HuggingFace profile when they go through config validation.
- Renamed the visible HuggingFace model to `TKI` and mapped it internally to
  `Qwen/Qwen2.5-1.5B-Instruct` for API calls.
- Added per-profile `send_temperature` and `send_top_p` switches and disabled
  both by default for the OpenAI Responses profile to avoid GPT-5.x sampling
  parameter incompatibilities.
- Migrated the local user config after writing
  `~/.config/telachat/config.toml.bak-0.49.0-provider-cleanup`.

## Verification

- `PYTHONPATH=src python3 -m telachat profiles --json`
- `PYTHONPATH=src python3 -m unittest tests.test_config tests.test_client tests.test_cli.CliTests.test_init_and_profiles tests.test_cli.CliTests.test_folders_command_manages_system_prompts tests.test_controller.ControllerTests.test_folder_system_prompt_is_used_for_new_session -v`
- `make check`
- `make zipapp install`
- `~/.local/bin/telachat --version`
- `python3 dist/telachat.pyz --version`

## 0.50.0 Skill watchdog

- Added `telachat skill-watchdog` to compact oversized Codex Skill
  frontmatter descriptions while preserving the full Skill body.
- Tk and GTK should only start the watchdog with
  `TELACHAT_ENABLE_SKILL_WATCHDOG=1`; the first implementation accidentally
  started it by default.
- The accidental live compactions were restored from the generated
  `SKILL.md.telachat-watchdog.bak` backups.

## 0.50.0 Verification

- `PYTHONPATH=src python3 -m telachat skill-watchdog --json`
- `PYTHONPATH=src python3 -m unittest tests.test_skill_watchdog tests.test_cli.CliTests.test_skill_watchdog_command_compacts_oversized_descriptions -v`
- `make check`

## 0.51.0 Theme expansion

- Added metallic/glass themes: `graphite-glass`, `liquid-chrome`, `black-ice`,
  and `brushed-steel`.
- Added aliases `glass`, `chrome`, `ice`, and `metallic`.
- Corrected the Tk/GTK Skill watchdog docs toward opt-in behavior while the
  implementation still needed a follow-up safety fix.

## 0.51.0 Verification

- `PYTHONPATH=src python3 -m unittest tests.test_skill_watchdog tests.test_themes -v`
- `make check`

## 0.51.1 Documentation correction

- Corrected docs that still described the Skill watchdog as opt-in; the code
  starts it by default in Tk/GTK and supports
  `TELACHAT_DISABLE_SKILL_WATCHDOG=1` as opt-out.

## Post-0.51.1 Skill watchdog safety correction

- Restored the Tk/GTK Skill watchdog opt-in gate because default-on GUI startup
  can mutate external Codex/Agent `SKILL.md` files under the user's home
  directory. The background loop now starts only with
  `TELACHAT_ENABLE_SKILL_WATCHDOG=1`; `TELACHAT_DISABLE_SKILL_WATCHDOG=1`
  still wins.
