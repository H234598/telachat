# Session: Telachat

Date: 2026-05-25

## Summary

- Built `Telachat`, a local OpenAI-compatible chat client.
- Added native GTK4 and Tk/ttk GUI frontends.
- Added built-in `openai` and `codex` profiles.
- Added `huggingface` provider alias and two-stage GUI selection: provider first, then model.
- Added collapsible left provider/chat panel and collapsible right system panel.
- Added chat folders/groups, chat pinning, folder filtering, sorting, and full-history search.
- Added GUI slash commands in the prompt composer for fast keyboard control.
- Added `Shift+Enter` send shortcut in both GTK and Tk; plain `Enter` keeps
  inserting line breaks.
- Fixed the SQLite worker-thread error by making the store connection
  thread-shareable and serializing SQLite access with a re-entrant lock.
- Replaced fixed side columns with broad, draggable split panes in GTK and Tk.
- Local source: `/home/teladi/telachat`
- Installed command: `/home/teladi/.local/bin/telachat`
- GUI commands: `/home/teladi/.local/bin/telachat-gtk`, `/home/teladi/.local/bin/telachat-tk`
- Zipapp artifact: `/home/teladi/telachat/dist/telachat.pyz`
- Local PyInstaller Tk bundle: `/home/teladi/telachat/dist/TelachatTk`
- Default config: `/home/teladi/.config/telachat/config.toml`
- History DB: `/home/teladi/.local/share/telachat/history.sqlite3`

## Default Profile

- Profile: `tki`
- Label: `TKI`
- API base: `https://haggfraise-qwen2-5-1-5b-instruct-free.hf.space/v1`
- Model sent to API: `gpt-4`
- API key: local envfile `/home/teladi/.config/telachat/qwen.env`
  (`TELACHAT_QWEN_API_KEY`)

## Features

- `telachat init` creates default config.
- `telachat profiles` shows configured providers with redacted keys.
- `telachat ask "..."` sends one prompt.
- `telachat ask --save "..."` saves prompt/answer to SQLite.
- `telachat chat` starts an interactive session with slash commands.
- `telachat-gtk` starts the GTK4/Adwaita GUI.
- `telachat-tk` starts the Tk/ttk GUI.
- GTK and Tk GUIs can hide the left sidebar and the system prompt panel.
- GTK and Tk GUIs support folders, sorting, search, and slash commands:
  `/new`, `/rename TITLE`, `/delete`, `/pin`, `/unpin`, `/regen`, `/folder NAME`,
  `/rename-folder NAME`, `/delete-folder`, `/move NAME`, `/unfile`, `/sort ...`, `/search ...`,
  `/provider ...`, `/model ...`, `/left`, `/system`.
- GTK and Tk GUIs can regenerate the latest assistant answer without duplicating
  the previous user message.
- GTK and Tk GUIs include a Pin/Unpin control; pinned chats are listed first.
- GTK and Tk composer shortcut: `Shift+Enter` sends the current message.
- GTK and Tk side panes are width-adjustable with wide splitter handles.
- `openai` uses Telachat's own envfile secret.
- `codex` uses the local `codex exec` CLI and is not a `/v1` HTTP backend.
- The OpenAI profile uses the OpenAI Responses API mode.
- The local config backup before profile migration is `/home/teladi/.config/telachat/config.toml.bak-20260525-gui-profiles`.
- The OpenAI key was copied into Telachat's own secret file `/home/teladi/.config/telachat/openai.env` with mode `0600`; the OpenAI profile now uses `envfile:/home/teladi/.config/telachat/openai.env#OPENAI_API_KEY`.
- The backup before switching OpenAI to envfile is `/home/teladi/.config/telachat/config.toml.bak-20260525-openai-envfile`.
- The backup before switching to Telachat's own envfile is `/home/teladi/.config/telachat/config.toml.bak-20260525-own-openai-env`.
- `telachat sessions` lists saved sessions.
- `telachat sessions --query TEXT`, `--folder NAME`, and `--sort ...` filter
  saved sessions from the CLI.
- `telachat export <id>` exports a session as Markdown.
- `telachat doctor` checks config and `/models`.
- `telachat doctor --chat` also sends a short live chat request.
- OpenAI-compatible streaming SSE and non-streaming chat are supported.
- Provider profiles are configurable in TOML and can use `env:NAME` for real secrets.
- The Qwen/HF Space API is now protected by the Space secret
  `TELACHAT_API_KEYS`; Telachat uses the local `Telachat_API_Teladi` value via
  `/home/teladi/.config/telachat/qwen.env`, and `Telachat_API_ReMeLi` was shown
  once in a separate terminal.
- The old placeholder key `hf-space` is rejected by the Space.
- Bavarder was updated to use the same local `Telachat_API_Teladi` key for its
  HF-backed `openai-gpt-35-turbo`, `local`, and `openai-gpt-4` providers.
- Bavarder config backup:
  `/home/teladi/.var/app/io.github.Bavarder.Bavarder/data/bavarder/data.json.bak-20260525-qwen-key-rotation`.
- Bavarder `data.json` and same-directory `data.json.bak-*` files were set to
  mode `0600`, because they can contain provider secrets.

## Verification

- Bytecode compile passed: `python3 -m compileall -q src tests`.
- Offline fake-API tests passed three times after GUI/profile work: 13 tests per run, 39 total checks.
- After envfile support, offline fake-API tests passed three times again: 14 tests per run, 42 total checks.
- Build passed: `python3 -m zipapp src -m telachat:main ...`.
- Installed wrapper passed `telachat --help`.
- Zipapp passed `python3 dist/telachat.pyz --help`.
- GUI modules import successfully.
- `python3-tkinter` was installed and verified.
- `telachat-gtk` and `telachat-tk` both launched under the desktop session and were auto-stopped by timeout.
- After provider/model selection, both GUI frontends launched again without visible errors.
- New `huggingface` alias live test returned `OK`.
- Project-local PyInstaller was installed in `/home/teladi/telachat/.venv-build`.
- Local Linux PyInstaller Tk bundle was built and its help output passed.
- Live `/models` check returned `gpt-3.5-turbo` and `Qwen/Qwen2.5-1.5B-Instruct`.
- Live `/chat/completions` check returned `OK`.
- Live saved ask test for `812 - 512` returned `300` and exported correctly.
- Live Codex bridge test returned `CODEX-OK`.
- OpenAI live tests passed through Responses API after loading the key from the local envfile:
  - `openai` profile with `gpt-5.4-mini` returned `OK`.
- After folder/search/responsive GUI work, offline tests passed three more
  times: 15 tests per run, 45 total checks.
- GTK and Tk GUI smoke tests launched and were stopped by timeout after the
  event loop stayed alive.
- The installed wrapper live-check against the default HF/TKI profile passed:
  `/models` returned `gpt-3.5-turbo` and `Qwen/Qwen2.5-1.5B-Instruct`,
  `/chat/completions` returned `OK`.
- After SQLite/thread, splitter and rename/delete GUI work, bytecode compile
  passed again.
- Focused tests passed again: SQLite worker-thread test plus GTK/Tk import
  tests.
- Full deterministic suite passed three more times after the latest GUI/store
  changes: 16 tests per run, 48 total checks.
- Chat pinning was added after the first final verification pass:
  - SQLite schema migration adds `sessions.pinned`.
  - CLI `sessions` marks pinned chats with `*`.
  - CLI and GUI slash commands support `/pin` and `/unpin`.
  - GTK/Tk GUIs expose a Pin/Unpin button and list pinned chats first.
  - Pinning order is covered by deterministic store tests.
  - Legacy DB migration from a pre-`pinned` schema is covered by a regression
    test.
- After pinning, bytecode compile passed again and full deterministic tests
  passed three more times: 17 tests per run, 51 total checks.
- After pinning, Zipapp, installed wrappers, and the local PyInstaller Tk bundle
  were rebuilt and help-checked again.
- After pinning, `telachat sessions --limit 3` passed against the real local DB,
  which also exercised the live schema migration.
- After pinning, GTK and Tk GUI smoke tests launched under the desktop session
  and were stopped by timeout without runtime errors.
- After pinning, live `telachat doctor --chat` against the HF/TKI profile passed
  again.
- CLI session listing was extended after pinning:
  - `telachat sessions --query TEXT`
  - `telachat sessions --folder NAME`
  - `telachat sessions --sort newest|oldest|title|title-desc|provider`
  - Deterministic CLI coverage verifies search, folder filtering, sort order,
    and pinned-first ordering.
- Answer regeneration was added as version `0.2.0` work:
  - Store can delete the latest assistant message only when it is the final
    message.
  - Controller regenerates from existing history without duplicating the user
    message.
  - CLI chat supports `/regen` and `/regenerate`.
  - GTK/Tk GUIs expose a Regenerieren button and `/regen` command.
  - Deterministic tests cover controller and CLI regeneration behavior.
- GitHub repo workflow was introduced:
  - Private repo: `https://github.com/H234598/telachat`
  - Semantic Versioning is documented in `VERSIONING.md`.
  - Version is `0.2.0` in `pyproject.toml`.
  - `CHANGELOG.md` tracks user-visible changes per version.
  - `.gitignore` excludes virtualenvs, build outputs, caches, and local DBs.
- After regeneration and repository setup, full deterministic tests passed three
  more times: 20 tests per run, 60 total checks.
- After regeneration, Zipapp, installed wrappers, and the local PyInstaller Tk
  bundle were rebuilt again.
- After regeneration, final checks passed:
  - installed wrapper help
  - zipapp help
  - PyInstaller Tk bundle help
  - GTK/Tk smoke launches stopped by timeout without runtime errors
  - live `telachat doctor --chat` against HF/TKI
- Prompt templates were added as version `0.3.0` work:
  - `[prompt_templates]` in `config.toml`.
  - Default templates: `summarize`, `explain`, `translate_de`.
  - CLI command `telachat templates`.
  - `telachat ask --template NAME ...`.
  - Interactive CLI and GUI commands `/templates` and `/template NAME TEXT`.
  - GTK/Tk template selectors insert templates into the composer.
  - Deterministic tests cover config loading, template application, CLI listing,
    and templated asks.
- GitHub Wiki should be kept alongside README/docs for compact user-facing
  documentation. Do not include secrets, logs, tokens, or raw local config.
  Source pages are maintained under `/home/teladi/telachat/docs/wiki`.
  GitHub reports `hasWikiEnabled=true`, but
  `https://github.com/H234598/telachat.wiki.git` currently returns
  `Repository not found` for clone and push attempts, so direct Wiki sync is
  pending GitHub-side initialization.
- After prompt templates, bytecode compile passed and full deterministic tests
  passed three more times: 23 tests per run, 69 total checks.
- After prompt templates, Zipapp, installed wrappers, and the local PyInstaller
  Tk bundle were rebuilt again.
- After prompt templates, final checks passed:
  - installed wrapper help
  - zipapp help
  - PyInstaller Tk bundle help
  - `telachat templates`
  - `telachat doctor --chat`
  - GTK/Tk smoke launches stopped by timeout without runtime errors
  - `git diff --check`
- Folder project prompts and provider consolidation were added as version
  `0.4.0` work:
  - SQLite `folders.system_prompt` with legacy migration.
  - `telachat folders` for folder listing, creation, prompt display, and prompt
    updates.
  - `/folder-system TEXT` in CLI, GTK, and Tk command paths.
  - GTK/Tk `Ordner-Prompt` action saves the visible system prompt as the
    selected folder default.
  - New sessions created in a folder inherit the folder prompt unless the user
    explicitly changes the system prompt.
  - The redundant `chatgpt` provider was removed; `openai` covers the OpenAI
    Responses API and GPT-5.x model choices.
  - Local config backup before removing the provider:
    `/home/teladi/.config/telachat/config.toml.bak-20260525-remove-chatgpt`.
- After `0.4.0` work, bytecode compile passed, focused tests passed, and the
  full deterministic suite passed three more times: 26 tests per run, 78 total
  checks.
- After `0.4.0` work, Zipapp, installed wrappers, and the local PyInstaller Tk
  bundle were rebuilt again.
- After `0.4.0` work, final checks passed:
  - installed wrapper help
  - zipapp help
  - PyInstaller Tk bundle help
  - `telachat profiles` shows `codex`, `huggingface`, `openai`, and `tki` only
  - `telachat folders --show-system`
  - `telachat doctor --chat`
  - GTK/Tk smoke launches stopped by timeout without runtime errors
  - `git diff --check`
  - secret scan only matched test fixtures
- After CLI session filtering/sorting, full deterministic tests passed three
  more times: 18 tests per run, 54 total checks.
- After CLI session filtering/sorting, Zipapp, installed wrappers, and the local
  PyInstaller Tk bundle were rebuilt again.
- Final installed-wrapper checks passed:
  - `telachat sessions --sort title`
  - `telachat sessions --query 812`
  - `telachat doctor --chat`
  - GTK/Tk smoke launches stopped by timeout without runtime errors.
- Rebuilt and verified:
  - Zipapp `/home/teladi/telachat/dist/telachat.pyz`
  - Installed wrappers under `/home/teladi/.local/bin`
  - Local PyInstaller Tk bundle `/home/teladi/telachat/dist/TelachatTk`
- Help checks passed for installed wrapper, zipapp, and PyInstaller Tk bundle.
- GTK and Tk GUI smoke tests launched under the desktop session and were stopped
  by timeout after their event loops stayed alive.
- Live key-rotation check passed:
  - New Teladi key: `/models` and `/chat/completions` OK through `telachat doctor --chat`
  - Old `hf-space` placeholder: HTTP 403
- Bavarder config check confirmed all HF-backed providers match the Teladi key
  and none uses `hf-space`.
- Hugging Face Space commit for API-key auth:
  `https://huggingface.co/spaces/HaggFraise/qwen2-5-1-5b-instruct-free/commit/081ebb4ac244dae7d648c858b6b8b9f874b4fa41`.

## Windows Packaging

- Windows packaging target is the Tk frontend.
- Build script: `/home/teladi/telachat/packaging/windows/build-tk-windows.ps1`
- NSIS script: `/home/teladi/telachat/packaging/windows/telachat-tk.nsi`
- Important caveat: PyInstaller is not a cross-compiler; a real Windows `.exe` and `.exe` installer must be built on Windows.
- Fedora did not provide `nsis` or `python3-pyinstaller` packages from the enabled repos; PyInstaller was installed locally via pip for the Linux packaging smoke test.

## Important Rules

- Do not buy Hugging Face credits unless explicitly instructed.
- Do not store real API tokens in Gists.
- Do not upload raw local config or SQLite history.

## References

- Local README: `/home/teladi/telachat/README.md`
- Research notes: `/home/teladi/telachat/docs/RESEARCH.md`
- Architecture: `/home/teladi/telachat/docs/ARCHITECTURE.md`
- Testing: `/home/teladi/telachat/docs/TESTING.md`
